Clonar repo en una ruta en especifico con git bash
- git clone #url de repositorio


Si quieres jalar commits del repo 
- git pull origin main

Si quieres subir commits al repo
- git add .
- git commit -m "nombre del commit"
- git push origin main
